<resources>

    <!-- Base application theme. -->
    <!--     <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"> -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <!--        <item name="android:colorPrimary">@color/primary</item>-->
        <!--   darker variant for the status bar and contextual app bars -->
        <!--        <item name="android:colorPrimaryDark">@color/primary_dark</item>-->
        <!--   theme UI controls like checkboxes and text fields -->
        <!--<item name="android:colorAccent">@color/accent</item>-->

    </style>

    <style name="ListToggleButton">
        <item name="android:textOn" />
        <item name="android:textOff" />
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>

    </style>

    <style name="CanCallToggleButton" parent="ListToggleButton">
        <item name="android:background">@drawable/call_toggle</item>
    </style>

    <style name="CanMessageToggleButton" parent="ListToggleButton">
        <item name="android:background">@drawable/message_toggle</item>
    </style>

    <style name="ListSeparator">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/black</item>
        <!--        <item name="android:background">?android:attr/listDivider</item> -->
    </style>

</resources>
