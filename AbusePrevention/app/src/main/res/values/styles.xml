<resources>

    <!-- Base application theme. -->
    <!--     <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar"> -->
    <style name="AppTheme" parent="android:Theme.Material">
        <item name="android:textViewStyle">@style/TextView</item>
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
<!--        <item name="android:colorPrimary">@color/primary</item>-->
        <!--   darker variant for the status bar and contextual app bars -->
<!--        <item name="android:colorPrimaryDark">@color/primary_dark</item>-->
        <!--   theme UI controls like checkboxes and text fields -->
        <!--<item name="android:colorAccent">@color/accent</item>-->

    </style>

    <style name="TodoButton"></style>

    <style name="TextView" parent="android:TextAppearance.Widget.TextView">
        <item name="android:color">@android:color/black</item>
        <item name="android:height">200dp</item>
        <item name="android:layout_height">200dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">12pt</item>
    </style>

    <style name="ListToggleButton">
        <item name="android:textOn" />
        <item name="android:textOff" />
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>

    </style>

    <style name="CanCallToggleButton">
        <item name="android:background">
            <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
                <item android:id="@+android:id/background" android:drawable="@android:color/transparent" />
                <item android:id="@+android:id/toggle" android:drawable="@drawable/call_toggle" />
            </layer-list>
        </item>
    </style>

    <style name="CanMessageToggleButton">
        <item name="android:background">
            <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
                <item android:id="@+android:id/background" android:drawable="@android:color/transparent" />
                <item android:id="@+android:id/toggle" android:drawable="@drawable/text_toggle" />
            </layer-list>
        </item>
    </style>

</resources>
