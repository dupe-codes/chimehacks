package chimehack.abuseprevention.function.triggers;

import android.content.Context;

/**
 * A trigger that's activated once the user shakes the phone three times.
 */
public class ShakeThriceTrigger implements Trigger {
    @Override
    public boolean isActivated(Context context) {
        // TODO(oleg): implement.
        return false;
    }
}
