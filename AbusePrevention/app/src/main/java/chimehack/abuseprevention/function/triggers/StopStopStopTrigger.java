package chimehack.abuseprevention.function.triggers;

import chimehack.abuseprevention.service.ChimeService;

/**
 * A trigger that's activated once the user says stop three times.
 */
public class StopStopStopTrigger implements Trigger {
    @Override
    public boolean isActivated(ChimeService service) {
        // TODO(oleg): implement.
        return false;
    }
}
