package chimehack.abuseprevention.function.triggers;

import chimehack.abuseprevention.service.ChimeService;

/**
 * A trigger that's activated once the user presses the volume button in a certain pattern.
 */
public class VolumeButtonPatternTrigger implements Trigger {
    @Override
    public boolean isActivated(ChimeService service) {
        // TODO(oleg): implement.
        return false;
    }
}
